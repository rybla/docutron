use anyhow::Result;

/// Fetch the bookmark urls via [`crate::utility::url_list::read_bookmarks_url_list`], then insert those urls into the database.
pub async fn fetch_bookmarks() -> Result<()> {
    unimplemented!()
}
