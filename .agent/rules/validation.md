---
trigger: always_on
---

After making your changes, run the command `cargo run --bin validate` to run automated code validation checks. You must address ALL diagnostics emitted from these checks.

If all checks pass, then follow the instructions to write a brief summary of your changes.
