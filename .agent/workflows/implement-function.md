---
description: Implement function
---

Consider the specified function outline in the source code. Pay close attention to the function's documentation comment and type signature.

Write the finalized implementation of this function. While doing so, you must respect the function's type signature -- keep the function's type signature fixed.

Once you've finished implementing the function, flush out the function's documentation comment to reflect the final implementation.

Finally, do @.agent/rules/validation.md
